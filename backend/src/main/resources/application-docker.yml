spring:
  application:
    name: Assignment

  # PostgreSQL Database Configuration (Cloud - Aiven)
  datasource:
    url: jdbc:postgresql://sksazid-sksazid.i.aivencloud.com:20700/defaultdb?sslmode=require
    driver-class-name: org.postgresql.Driver
    username: avnadmin
    password: AVNS_T2d6b90dS052GAilGq-
  
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
  expiration: ${JWT_EXPIRATION:86400000}
  refresh-expiration: ${JWT_REFRESH_EXPIRATION:604800000}

# Logging
logging:
  level:
    org:
      springframework:
        security: INFO
